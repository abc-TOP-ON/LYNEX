-- تحسين الأداء ومنع التقطيع على الهواتف
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LynexProSearchMenu"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

-- حماية السكربت
if syn and syn.protect_gui then 
	syn.protect_gui(ScreenGui) 
	ScreenGui.Parent = CoreGui
elseif getgui then 
	ScreenGui.Parent = getgui()
else 
	ScreenGui.Parent = CoreGui 
end

-- 1. الظل الخارجي المضيء
local GlowFrame = Instance.new("Frame")
GlowFrame.Name = "GlowFrame"
GlowFrame.Size = UDim2.new(0, 380, 0, 530)
GlowFrame.Position = UDim2.new(0.5, -190, 0.5, -265)
GlowFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
GlowFrame.BackgroundTransparency = 0.65
GlowFrame.BorderSizePixel = 0
GlowFrame.Parent = ScreenGui

local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(0, 18)
GlowCorner.Parent = GlowFrame

-- 2. الإطار الرئيسي
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 370, 0, 520)
MainFrame.Position = UDim2.new(0.5, -185, 0.5, -260)
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 16)
MainCorner.Parent = MainFrame

-- 3. تأثير الإطار الأحمر النيون
local BorderGradient = Instance.new("UIGradient")
BorderGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
	ColorSequenceKeypoint.new(0.2, Color3.fromRGB(100, 0, 0)),
	ColorSequenceKeypoint.new(0.4, Color3.fromRGB(15, 15, 15)),
	ColorSequenceKeypoint.new(0.6, Color3.fromRGB(15, 15, 15)),
	ColorSequenceKeypoint.new(0.8, Color3.fromRGB(100, 0, 0)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
})
BorderGradient.Parent = MainFrame

-- 4. الواجهة الداخلية
local InsideFrame = Instance.new("Frame")
InsideFrame.Size = UDim2.new(1, -6, 1, -6)
InsideFrame.Position = UDim2.new(0, 3, 0, 3)
InsideFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
InsideFrame.BorderSizePixel = 0
InsideFrame.Parent = MainFrame

local InsideCorner = Instance.new("UICorner")
InsideCorner.CornerRadius = UDim.new(0, 14)
InsideCorner.Parent = InsideFrame

-- خط فاصل
local Line = Instance.new("Frame")
Line.Size = UDim2.new(1, -30, 0, 2)
Line.Position = UDim2.new(0, 15, 0, 52)
Line.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Line.BorderSizePixel = 0
Line.Parent = InsideFrame

-- دالة التدرج الأحمر
local function createRedTextGradient(parentFrame)
	local Gradient = Instance.new("UIGradient")
	Gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
		ColorSequenceKeypoint.new(0.3, Color3.fromRGB(255, 90, 90)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(130, 0, 0)),
		ColorSequenceKeypoint.new(0.7, Color3.fromRGB(255, 90, 90)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
	})
	Gradient.Parent = parentFrame
	return Gradient
end

-- 5. العنوان
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "LYNEX"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 26
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Center
Title.Parent = InsideFrame

local TitleGradient = createRedTextGradient(Title)

-- 6. زر الإغلاق
local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -40, 0, 10)
CloseButton.BackgroundColor3 = Color3.fromRGB(25, 20, 20)
CloseButton.BorderSizePixel = 0
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 50, 50)
CloseButton.TextSize = 16
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Parent = InsideFrame

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- 7. البحث
local SearchBorderFrame = Instance.new("Frame")
SearchBorderFrame.Name = "SearchBorderFrame"
SearchBorderFrame.Size = UDim2.new(0, 290, 0, 32)
SearchBorderFrame.Position = UDim2.new(0.5, -145, 0, 60)
SearchBorderFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
SearchBorderFrame.BorderSizePixel = 0
SearchBorderFrame.Parent = InsideFrame

local SearchBorderCorner = Instance.new("UICorner")
SearchBorderCorner.CornerRadius = UDim.new(0, 8)
SearchBorderCorner.Parent = SearchBorderFrame

local SearchBorderGradient = createRedTextGradient(SearchBorderFrame)

local SearchBar = Instance.new("TextBox")
SearchBar.Name = "SearchBar"
SearchBar.Size = UDim2.new(1, -4, 1, -4)
SearchBar.Position = UDim2.new(0, 2, 0, 2)
SearchBar.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
SearchBar.BorderSizePixel = 0
SearchBar.ClearTextOnFocus = false
SearchBar.PlaceholderText = "Search scripts..."
SearchBar.PlaceholderColor3 = Color3.fromRGB(120, 120, 120)
SearchBar.Text = ""
SearchBar.TextColor3 = Color3.fromRGB(255, 255, 255)
SearchBar.TextSize = 13
SearchBar.Font = Enum.Font.GothamBold
SearchBar.TextXAlignment = Enum.TextXAlignment.Center
SearchBar.Parent = SearchBorderFrame

local SearchCorner = Instance.new("UICorner")
SearchCorner.CornerRadius = UDim.new(0, 7)
SearchCorner.Parent = SearchBar

local SearchTextGradient = createRedTextGradient(SearchBar)

-- =============================================
-- 🔥 قائمة الأزرار تحت البحث (مثل الصورة)
-- =============================================

-- دالة إنشاء زر سكربت (لكل سكربت)
local function createScriptButton(parent, yPos, scriptName, description, scriptUrl, autoExecute)
	local buttonContainer = Instance.new("Frame")
	buttonContainer.Size = UDim2.new(1, -20, 0, 70)
	buttonContainer.Position = UDim2.new(0, 10, 0, yPos)
	buttonContainer.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
	buttonContainer.BorderSizePixel = 0
	buttonContainer.Parent = parent
	
	local containerCorner = Instance.new("UICorner")
	containerCorner.CornerRadius = UDim.new(0, 10)
	containerCorner.Parent = buttonContainer
	
	-- اسم السكربت (بالأعلى)
	local nameLabel = Instance.new("TextLabel")
	nameLabel.Size = UDim2.new(0.7, 0, 0, 22)
	nameLabel.Position = UDim2.new(0, 10, 0, 5)
	nameLabel.BackgroundTransparency = 1
	nameLabel.Text = scriptName
	nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	nameLabel.TextSize = 14
	nameLabel.Font = Enum.Font.GothamBold
	nameLabel.TextXAlignment = Enum.TextXAlignment.Left
	nameLabel.Parent = buttonContainer
	
	-- الوصف (بالأسفل)
	local descLabel = Instance.new("TextLabel")
	descLabel.Size = UDim2.new(0.7, 0, 0, 20)
	descLabel.Position = UDim2.new(0, 10, 0, 28)
	descLabel.BackgroundTransparency = 1
	descLabel.Text = description
	descLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
	descLabel.TextSize = 12
	descLabel.Font = Enum.Font.Gotham
	descLabel.TextXAlignment = Enum.TextXAlignment.Left
	descLabel.Parent = buttonContainer
	
	-- زر EXECUTE
	local execBtn = Instance.new("TextButton")
	execBtn.Size = UDim2.new(0, 75, 0, 30)
	execBtn.Position = UDim2.new(1, -85, 0, 20)
	execBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
	execBtn.BorderSizePixel = 0
	execBtn.Text = "EXECUTE"
	execBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	execBtn.TextSize = 12
	execBtn.Font = Enum.Font.GothamBold
	execBtn.Parent = buttonContainer
	
	local execCorner = Instance.new("UICorner")
	execCorner.CornerRadius = UDim.new(0, 6)
	execCorner.Parent = execBtn
	
	-- تدرج أحمر لزر التنفيذ
	createRedTextGradient(execBtn)
	
	-- زر AUTO (إذا كان autoExecute = true)
	if autoExecute then
		local autoBtn = Instance.new("TextButton")
		autoBtn.Size = UDim2.new(0, 45, 0, 20)
		autoBtn.Position = UDim2.new(1, -85, 0, 52)
		autoBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
		autoBtn.BorderSizePixel = 0
		autoBtn.Text = "AUTO"
		autoBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
		autoBtn.TextSize = 10
		autoBtn.Font = Enum.Font.GothamBold
		autoBtn.Parent = buttonContainer
		
		local autoCorner = Instance.new("UICorner")
		autoCorner.CornerRadius = UDim.new(0, 4)
		autoCorner.Parent = autoBtn
	end
	
	-- دالة تنفيذ السكربت
	local function executeScript()
		execBtn.Text = "⏳"
		execBtn.BackgroundColor3 = Color3.fromRGB(200, 150, 0)
		
		local success, result = pcall(function()
			return game:HttpGet(scriptUrl)
		end)
		
		if success and result then
			local loadSuccess, execError = pcall(function()
				local func = loadstring(result)
				if func then
					func()
					return true
				else
					return false
				end
			end)
			
			if loadSuccess then
				execBtn.Text = "✅"
				execBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
				task.wait(1.5)
			else
				execBtn.Text = "❌"
				execBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
				task.wait(1.5)
			end
		else
			execBtn.Text = "❌"
			execBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
			task.wait(1.5)
		end
		
		execBtn.Text = "EXECUTE"
		execBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
	end
	
	execBtn.MouseButton1Click:Connect(executeScript)
	execBtn.TouchTap:Connect(executeScript)
	
	return buttonContainer
end

-- قائمة السكربتات (أضف سكربتاتك هنا)
local scripts = {
	{
		name = "★★★ LOR LORLN SPAM CMD",
		desc = "HD Admin | CMD Bar | Full Control",
		url = "https://raw.githubusercontent.com/abc-TOP-ON/LYNEX/refs/heads/main/LYNEX%201",
		auto = false
	},
	{
		name = "★★ LOR LORLN ANTI SPAM",
		desc = "Anti Spam Protection System",
		url = "https://raw.githubusercontent.com/abc-TOP-ON/LYNEX/refs/heads/main/LYNEX%202",
		auto = false
	},
	{
		name = "★ LOR LORLN SPEED",
		desc = "Speed hack for all games",
		url = "https://raw.githubusercontent.com/abc-TOP-ON/LYNEX/refs/heads/main/LYNEX%203",
		auto = false
	}
}

-- إنشاء الأزرار في القائمة
local scrollContainer = Instance.new("ScrollingFrame")
scrollContainer.Size = UDim2.new(1, 0, 0, 320)
scrollContainer.Position = UDim2.new(0, 0, 0, 105)
scrollContainer.BackgroundTransparency = 1
scrollContainer.BorderSizePixel = 0
scrollContainer.ScrollBarThickness = 4
scrollContainer.ScrollBarImageColor3 = Color3.fromRGB(180, 0, 0)
scrollContainer.Parent = InsideFrame

-- حاوية داخلية للازرار
local buttonHolder = Instance.new("Frame")
buttonHolder.Size = UDim2.new(1, 0, 0, #scripts * 80 + 20)
buttonHolder.Position = UDim2.new(0, 0, 0, 0)
buttonHolder.BackgroundTransparency = 1
buttonHolder.Parent = scrollContainer

scrollContainer.CanvasSize = UDim2.new(0, 0, 0, #scripts * 80 + 20)

-- إنشاء كل زر
for i, scriptData in ipairs(scripts) do
	local yPos = (i - 1) * 80 + 5
	createScriptButton(buttonHolder, yPos, scriptData.name, scriptData.desc, scriptData.url, scriptData.auto)
end

-- =============================================
-- نهاية قائمة الأزرار
-- =============================================

-- وظيفة البحث (فلترة الأزرار)
SearchBar.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local searchText = SearchBar.Text:lower()
		-- هنا تقدر تضيف فلترة حسب النص
	end
end)

-- نظام الدوران
local function animate(targetGradient, speed)
	if targetGradient then
		local Tween = TweenService:Create(targetGradient, TweenInfo.new(speed, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), {Rotation = 360})
		Tween:Play()
	end
end

animate(BorderGradient, 2)
animate(TitleGradient, 1.5)
animate(SearchBorderGradient, 2)
animate(SearchTextGradient, 2.2)

-- نظام السحب
local dragging, dragInput, dragStart, startPos

local function update(input)
	local delta = input.Position - dragStart
	local targetPos = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	
	MainFrame.Position = targetPos
	GlowFrame.Position = UDim2.new(targetPos.X.Scale, targetPos.X.Offset - 5, targetPos.Y.Scale, targetPos.Y.Offset - 5)
end

MainFrame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragging = false end
		end)
	end
end)

MainFrame.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then update(input) end
end)

print("LYNEX UI Loaded Successfully!")
